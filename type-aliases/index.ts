// Type aliases são basicamente 'apelidos' que podemos definir para um tipo

type id = number | string;

let idCustomer: id;

idCustomer = 'g_loyola';
idCustomer = 101;

let idProduct: id;

idProduct = 'sword';
idProduct = 400;
